import React from 'react';
import styles from "./Main.module.scss"

export const Main = () => {
    return (
        <div className={styles.div}>
            Main
        </div>
    );
};

export default Main